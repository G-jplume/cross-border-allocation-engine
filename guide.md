# 分仓占比计算引擎 - 使用说明

上传历史出单数据 → 调节参数 → 计算各仓库发货占比 → 导出结果

四仓占比合计恒为 100% | 最大余额法保证落货量整数精确 | 自动体检防静默失效

---

## 1. 系统概述

### 1.1 为什么需要这个工具

分仓占比计算引擎是一款面向跨境电商运营人员的工具，用于计算各 SKU 在四个仓库（美西、美东、美南GA、美南TX）的发货占比。

公司现状：运算发货月份的分仓数据时，仅参考去年同期的订单数据。这一做法存在以下问题：

**问题一：非季节性产品缺乏合理性**
- 非季节性产品的分仓分布在各月份相对稳定，仅参考去年同一个月的数据样本量小、偶然性大
- 例如某SKU去年同月美西10单、美东8单，但全年看美西占60%、美东占40%，仅用同月数据会放大偶然偏差

**问题二：新品数据不稳定**
- 新品上架初期出单量小、分仓随机性大，直接照搬去年同期数据会导致分仓比例失真
- 特别是上架不足3个月的新品，几乎没有可参考的历史分仓数据

**问题三：其他年份数据未合理利用**
- 仅参考去年同月，前年同月和其他月份的数据被完全忽略
- 前年同月的数据对趋势判断有价值，其他月份的数据对非季节性产品有参考意义
- 大量历史数据闲置，未发挥应有的辅助作用

### 1.2 本工具如何解决这些问题

**针对问题一（非季节性产品）：全量数据+时间衰减加权**
- 非季节性产品使用全部历史月份的数据，而非仅去年同月
- 时间越近的数据权重越大（指数衰减），既充分利用历史数据又突出近期趋势
- 月份相似度加权：同月权重最高（1.0），相邻月次之（0.6），隔2月再次（0.3），隔3月以上最低（0.1）

**针对问题二（新品不稳定）：基准占比层级回退+贝叶斯收缩**
- 新品（历史月数≤2 或 目标期总单量<20）自身权重为0，完全使用基准占比
- 基准按 SPU → 一级分类 → 室内外 → 全公司 逐层回退，数据少的层级自动回退到更稳定的父层
- 每层通过贝叶斯收缩平衡自身数据与父层基准，数据量越大越信任自身，数据量越小越信任父层

**针对问题三（其他年份未利用）：双层加权体系**
- 第一层 — 时间衰减加权：同月跨年用 λ_same（0.95）衰减，非同月用 λ（0.80）衰减 + 月份相似度
- 第二层 — 趋势因子：比较去年同期与前年同期的分仓占比差，把趋势方向叠加到最终占比（动态α：≥100单用原始α，50-100单用α/2，<50单跳过）
- 两个维度协同：时间衰减充分利用所有历史数据，趋势因子捕捉年度变化方向

### 1.3 核心能力

- 时间衰减加权（含月份相似度）+ 趋势因子（含单量门槛+动态α）两层加权体系
- 基准占比层级回退：SPU → 一级分类 → 室内外 → 全公司，每层贝叶斯收缩
- 季节性分层检测：品类级自动推荐（分仓偏移量≥阈值）+ SKU级分层回退（SKU→SPU→品类→弱季节）
- 减仓优化：月均<3单且占比<5%的仓位自动减仓，占比等比分配到其他仓
- 四仓占比合计恒为 100%，落货量最大余额法保证整数精确
- 占比微调：拖动滑块调整四仓占比，自动补足到 100%，支持单SKU和批量微调
- 多维度导出：Excel（含聚合）、CSV（SKU级/全层级聚合/一级分类）

---

## 2. 页面结构总览

页面分为侧边栏（参数面板）和主区域（操作区）两部分。

### 侧边栏 — 参数面板

分为四个参数分组，从上到下依次为：
- ① 目标发货月份：选择起始年月和结束年月（如 2026年12月 → 2027年3月）
- ② 时间衰减加权：同月跨年衰减率 λ_same、非同月衰减率 λ
- ③ 趋势因子 α：趋势因子 α、单仓调整上限、趋势单量门槛（默认折叠）
- ④ 新品与基准：新品阈值、新品订单量门槛、收缩强度 k、权重上/下限、品类层观测数（默认折叠）

### 主区域 — 操作区

分为六个区块，从上到下依次为：
- 1. 上传数据：上传原始出单数据（CSV/Excel）或使用示例数据
- 2. 开始计算：蓝色边框容器内含季节适用品类多选、SKU级分仓偏移自动检测、减仓优化开关、计算按钮；右上角有下载说明按钮（Markdown 格式）
- 3. 计算结果：自动体检、KPI卡片（四仓加权平均占比）、SKU级表格（6个Tab含计算路径）、聚合层级占比
- 4. 占比微调：默认折叠，支持单个SKU微调和批量微调，滑块调整四仓占比，自动补足到 100%，生成落货量
- 5. 可视化：饼图、可搜索堆叠图（页码下拉框翻页）
- 6. 导出结果：Excel（含聚合）、CSV（SKU级）、CSV（全层级聚合）、CSV（一级分类）

---

## 3. 侧边栏参数说明

### 3.1 目标发货月份

选择起始年月和结束年月，每个下拉框直接显示「2027年1月」这样的年月组合。支持跨年（如 2026年12月 → 2027年3月）。

锚点 = 目标期中间月份。强季节品类只看目标期同月数据，弱季节品类放开全部月份。

### 3.2 时间衰减加权

控制历史数据的影响力。同月数据跨年衰减用 λ_same，非同月数据用 λ。

| 参数 | 默认值 | 说明 |
|---|---|---|
| 同月跨年衰减率 λ_same | 0.95 | 同月数据跨年衰减。去年同月保留95%，前年同月保留90% |
| 非同月衰减率 λ | 0.80 | 弱季节品类的非同月数据按此衰减。1个月前80%，6个月前26% |

分类逻辑：
- 强季节品类（庭院等）：只看目标期同月数据，用 λ_same 衰减
- 弱季节品类：同月数据用 λ_same 衰减，非同月数据用 λ 衰减
- 强季节品类的非同月数据：层级回退时用 λ 衰减（无同月数据时启用）

### 3.3 趋势因子 α

比较「去年同期」与「前年同期」的分仓占比差，把趋势方向叠加到最终占比上。使用原始单量占比计算。

| 参数 | 默认值 | 说明 |
|---|---|---|
| 趋势因子 α | 0.3 | 趋势差 × α 叠加到最终占比。α=0 关闭 |
| 单仓调整上限 | 0.05 | 每个仓库的趋势调整幅度不超过 ±5% |
| 趋势单量门槛 | 50 | 去年+前年同期总单量低于此值跳过趋势调整 |

### 3.4 新品与基准

控制新品判定和自身数据与基准的混合比例。

| 参数 | 默认值 | 说明 |
|---|---|---|
| 新品阈值（月） | 2 | 历史出单月数 ≤ 此值的 SKU 视为新品，完全使用基准占比 |
| 新品订单量门槛 | 20 | 目标期总单量 < 此值也视为新品，避免低单量SKU分仓偶然性 |
| 收缩强度 k | 8 | 自身权重 = n / (n + k)，n = 目标期月数。k越大越信任基准 |
| 权重下限 | 0.0 | 自身数据的最低权重。0=新品完全用基准 |
| 权重上限 | 0.85 | 自身数据的最高权重。0.85=最多85%用自身 |
| 品类层最小等效观测数 | 12 | 基准贝叶斯收缩参数。n=12时子层父层各50% |

新品判定逻辑：历史月数 ≤ 阈值 或 目标期总单量 < 门槛 → 视为新品，自身权重=0，完全使用基准占比。门槛默认20单，避免低单量SKU分仓偶然性。

---

## 4. 主区域操作指南

### 4.1 上传数据

点击「上传原始出单数据」或使用示例数据。必需列：源SKU、SPU、室内外、一级分类、年、月、美西、美东、美南GA、美南TX。

数据格式示例：

| 源SKU | SPU | 室内外 | 一级分类 | 年 | 月 | 美西 | 美东 | 美南GA | 美南TX |
|---|---|---|---|---|---|---|---|---|---|
| NBGF050BROOS-V4 | NBGF050BROOS | 室内 | 庭院 | 2026 | 5 | 12 | 8 | 3 | 2 |

上传后可展开「数据体检 + 预览」查看总行数、SKU数、品类数、时间跨度。

### 4.2 开始计算

在主页面蓝色边框容器内设置：
- 季节适用品类：多选框，默认根据分仓偏移量自动推荐强季节品类（偏移量≥15%的品类默认勾选），用户可手动增减
- SKU级分仓偏移自动检测：三个滑块（强季节偏移阈值5-50%默认20%，弱季节偏移阈值1-30%默认10%，品类层偏移阈值5-40%默认15%）。系统算每个SKU目标期vs非目标期的四仓占比差异（偏移量=四仓占比差值绝对值之和）。偏移≥强阈值→强季节，≤弱阈值→弱季节，中间→回退到SPU层→品类层
- 分层检测逻辑：SKU自身数据（目标期和非目标期各≥50单）→SPU层（同条件）→品类层→弱季节（数据不足时用全部月份加权）
- 减仓优化：勾选开关，月均单量上限和占比上限可自定义，启用后符合条件自动减仓
- 点击「开始计算」按钮，系统分5步逐步显示计算进度

### 4.3 计算结果

计算完成后展示：
- 自动体检：四仓占比合计=100%、季节性判定生效（含分仓偏移量统计）、趋势因子生效数（含动态α）
- KPI卡片：美西、美东、美南GA、美南TX 四仓加权平均占比（按各SKU目标期出单量加权，含与基准的差值）
- SKU级表格：6个分页Tab（调整后/最终/自身/基准/趋势差/计算路径），基础列含季节偏移量和季节性来源；计算路径Tab可查看单个SKU的基本信息、计算参数和四仓占比明细
- 聚合层级：一级分类、室内外、SPU、全公司四个 Tab，展示各层级的分仓占比（按单量加权平均）

### 4.4 占比微调

默认折叠，点击展开。支持两种微调模式：
- **单个SKU微调**：选择要微调的 SKU，拖动四个滑块调整占比（显示为整数百分比），实时显示四仓合计是否=100%，点击「自动补足到 100%」以改动最大的仓为准其余三仓按原比例补足，点击「🔄 恢复引擎值」一键回到系统计算结果，输入发运批量按修正后的占比生成落货量（最大余额法），可下载该 SKU 的调整结果 CSV
- **批量微调**：选择品类/SPU/室内外维度，支持「整体偏移」（四仓各设增减百分点，合计需为0）或「直接设定占比」（四仓合计需为100%），一键应用到该组所有 SKU，可清除批量调整

### 4.5 可视化

- 饼图：展示单个 SKU 的四仓占比，含标签和百分比
- 堆叠图：支持按 SKU 名搜索，页码下拉框翻页浏览全部SKU，合计恒为 100%
- 聚合堆叠图：一级分类和室内外层级也展示堆叠柱状图

### 4.6 导出结果

- Excel（含聚合）：SKU级占比 + 一级分类/室内外/SPU/全公司聚合
- CSV（SKU级）：仅 SKU 级占比数据
- CSV（全层级聚合）：一级分类+室内外+SPU+全公司，按层级列区分
- CSV（一级分类）：仅一级分类聚合占比

---

## 5. 计算逻辑说明

计算链路共5步，点击「开始计算」后逐步显示进度：

### 步骤1：运算SKU映射
混用SKU映射表把源SKU合并到相似SKU；未映射的取「-」前部分作为运算SKU。

### 步骤2：时间衰减加权
时间衰减 × 月份相似度。同月数据用 λ_same（0.95）跨年衰减。非同月数据用 λ（0.80）衰减，再乘月份相似度权重（同月1.0/相邻月0.6/隔2月0.3/隔3月以上0.1），让目标月附近的非同月数据也有参考价值。

### 步骤3：自身占比（含分仓偏移分层检测）
加权后的各仓出单量 ÷ 加权合计，逐仓计算。

系统先算每个SKU的"分仓偏移量"=目标期四仓占比 vs 非目标期四仓占比的差异（绝对值之和），再分层判断：
- 第1层：SKU自身偏移量（目标期和非目标期各≥50单时）→ 偏移≥20%强季节，≤10%弱季节
- 第2层：SPU层偏移量（同条件时）→ 同样阈值
- 第3层：品类层偏移量 → ≥15%强季节，≤10%弱季节
- 第4层：以上都不满足 → 弱季节，用全部月份加权

强季节只用目标期同月数据，弱季节用全部月份加权。中间地带（10%-20%）回退到下一层判断。

这样即使庭院冬天单量少但美南占比明显偏高（强季节特征），也能被正确识别，不会被"单量少"误判为弱季节。

### 步骤4：基准占比
层级回退 + 贝叶斯收缩（全部从导入数据动态计算）：

| 优先级 | 基准来源 | 说明 |
|---|---|---|
| 1 | SPU | 贝叶斯收缩，父层=所属一级分类 |
| 2 | 一级分类 | 贝叶斯收缩，父层=全公司 |
| 3 | 室内外 | 贝叶斯收缩，父层=全公司 |
| 4 | 全公司 | 整体占比兜底 |

贝叶斯收缩公式：子层权重 = n / (n + k_cat)，父层权重 = k_cat / (n + k_cat)。k_cat=12时，n=12则子层父层各50%。

### 步骤5：最终占比 + 落货量 + 减仓优化
最终占比 = 自身权重 × 自身占比 + (1-自身权重) × 基准占比，再叠加趋势调整，最后归一化到 100%。

新品（历史月数≤2 或 目标期原始单量<20）自身权重=0，完全使用基准。

趋势调整用原始单量占比，比较去年同期 vs 前年同期的占比差。单量门槛+动态α：去年+前年同期总单量≥100用原始α，50-100单用α/2，<50单或任一侧为0单跳过趋势调整。

落货量 = 占比 × 批量，最大余额法保证整数合计精确等于批量。

减仓优化（可选）：启用后，对运算SKU × 仓点判断月均单量和占比是否低于自定义阈值，符合条件则减仓，占比等比分配到其他仓。仅影响SKU层，上层聚合使用减仓前原始数据。

---

## 6. 常见问题

### Q1：目标期设定为5月，趋势因子参考哪些时间？
趋势因子比较去年同期（2026年5月）与前年同期（2025年5月）的原始单量占比差。多月目标期（如4-6月）则参考4-6月。

### Q2：强季节和弱季节品类怎么区分？
系统算"分仓偏移量"=目标期四仓占比 vs 非目标期四仓占比的差异（绝对值之和）。偏移量大说明分仓分布随季节变化大→强季节，偏移小说明分仓稳定→弱季节。分层检测：先看SKU自身数据（需目标期和非目标期各≥50单），不够回退到SPU层→品类层。SKU/SPU层偏移≥20%→强，≤10%→弱；品类层≥15%→强。强季节只用目标期同月数据，弱季节用全部月份加权。阈值可在"开始计算"区域滑块调节。

### Q3：同月跨年衰减率 λ_same 和非同月衰减率 λ 有什么区别？
λ_same（0.95）用于同月数据跨年衰减，去年同月保留95%，前年同月保留90%。λ（0.80）用于弱季节品类的非同月数据衰减，1个月前80%，6个月前26%。同月数据参考价值更高，所以衰减更慢。

### Q4：新品怎么判定？
历史出单月数 ≤ 2 或 目标期总单量 < 20，满足任一即视为新品。新品自身权重=0，完全使用基准占比（从 SPU 到全公司逐级回退）。

### Q5：减仓优化影响上层聚合吗？
不影响。减仓仅修改 SKU 层的「调整后」列，一级分类/室内外/SPU/全公司的聚合使用减仓前的原始数据。

### Q6：四仓占比一定等于100%吗？
是的。引擎计算后自动归一化，趋势调整后再次归一化，微调时自动补足，自检验证偏差 < 1e-9。

### Q7：趋势因子用什么数据计算？低单量SKU会怎样？
趋势因子用原始单量占比（不加衰减权重），比较去年同期 vs 前年同期的分仓占比差。动态α：≥100单用原始α，50-100单用α/2，<50单跳过趋势调整。与时间衰减加权作用层不同，互不干扰。
